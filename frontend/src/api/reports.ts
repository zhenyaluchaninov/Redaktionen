/**
 * Declares placeholder requests for the reports domain.
 */
import type { ContentItem } from "../types";

export const fetchReports = async (): Promise<ContentItem[]> => {
  throw new Error("fetchReports is not implemented yet.");
};
