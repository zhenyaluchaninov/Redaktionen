/**
 * Declares placeholder requests for the summaries domain.
 */
import type { ContentItem } from "../types";

export const fetchSummaries = async (): Promise<ContentItem[]> => {
  throw new Error("fetchSummaries is not implemented yet.");
};
