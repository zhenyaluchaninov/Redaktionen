/**
 * Declares placeholder requests for the signals domain.
 */
import type { ContentItem } from "../types";

export const fetchSignals = async (): Promise<ContentItem[]> => {
  throw new Error("fetchSignals is not implemented yet.");
};
